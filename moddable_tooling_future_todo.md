# Moddable Tooling Future TODO

この文書は「現行ツールの運用工夫では吸収しきれない項目」をまとめた将来TODO。

## 優先度P0

| ID | TODO | 現行運用で吸収できない理由 | 期待する改修 |
|---|---|---|---|
| P0-1 | `testmc` のPNGスナップショット出力 | 現状はchecksum中心で、画像生成は手動依存 | `screen.captureImage(path)` 相当を追加し、テスト時に直接PNG保存 |
| P0-2 | `xsbug-log` の組み込みフィルタ（build/debug/test） | いまは後段のgrep/rg頼みで再利用性が低い | `--channel` / `--exclude` オプションを追加してログ分類を標準化 |
| P0-3 | ヘッドレス公式テストランナー | GUIのTestタブ依存が残る | CLIで `testmc` シナリオ選択・実行・結果出力を完結させる |

## 優先度P1

| ID | TODO | 現行運用で吸収できない理由 | 期待する改修 |
|---|---|---|---|
| P1-1 | `mcconfig` のログストリーム分離 | build/deploy/runtimeが1経路に混在しやすい | `--log-build` `--log-runtime` などの明示出力先オプション |
| P1-2 | 構造化ログ（JSON Lines）標準化 | 人手/AIでの解析前処理が毎回必要 | XSランタイム側で構造化ログAPIを提供 |
| P1-3 | ゴールデン更新コマンドの公式化 | 現在はプロジェクトローカルscriptでばらつく | `testmc` 公式コマンドで checksum+image 同時更新 |

## 優先度P2

| ID | TODO | 現行運用で吸収できない理由 | 期待する改修 |
|---|---|---|---|
| P2-1 | 画像差分レポートの自動生成 | checksum不一致時の原因特定が遅い | 失敗時に差分画像（before/after/diff）を自動出力 |
| P2-2 | E2EシナリオDSLの公式サポート | シナリオ記述がプロジェクト依存で再利用しづらい | touch/wait/assertを統一DSLで記述可能にする |
| P2-3 | CIテンプレートの標準提供 | 2段CI（sim+実機）の導入コストが高い | GitHub Actions向け公式テンプレートと成果物フォーマットを提供 |

## 受け入れ基準（Doneの定義）

1. 人が見て判断できる成果物（PNG/diff）が自動生成される。
2. AIがそのまま解析できる構造化ログが出力される。
3. GUIなしでテスト実行から結果収集まで完結できる。
4. PRで再利用可能な標準ワークフローとして運用できる。
