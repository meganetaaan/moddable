# piu-next 要求仕様書（ドラフト v0.1）

## 1. 文書目的
本仕様書は、`piu` をゼロベースで再設計し、モダンな組み込み向けUIライブラリ（以下 `piu-next`）を開発するための要求を定義する。

## 2. 背景と課題
現行 `piu` には以下の課題がある。

- `template` とインスタンス化モデルの理解コストが高い。
- `anchor` オブジェクトの引き回しが、参照の流れと責務を不明瞭にしている。
- `js + d.ts` 二重管理により型不整合が発生しやすい。
- `any` が残存し、API利用時の安全性と補完品質が低下する。

## 3. 目標
- 覚えやすく一貫性のあるAPIを提供する。
- 描画のメモリ効率・CPU効率を現状維持以上にする。
- C実装を最大活用し、JS実行を最小化する。
- 宣言的記述を標準化し、TSXを正式対応する（トランスパイル前提）。
- ネイティブTypeScript実装に統一する。
- アニメーションを標準機能として提供する。

## 4. 非目標
- Web DOM互換を目的としない。
- 汎用フロントエンドフレームワーク互換を目的としない。
- 初期リリースで現行 `piu` API の完全互換は必須としない。
- 実験版期間における後方互換性維持を目的としない。

## 5. 機能要件（FR）

| ID | 要件 | 優先度 | 受け入れ基準 |
|---|---|---|---|
| FR-001 | コンポーネント定義方式を単一モデルに統一する。 | Must | 公式ガイド上で定義方法が1系統で説明可能。 |
| FR-002 | インスタンス化APIを単純化し、`template` 由来の暗黙挙動を排除する。 | Must | 主要コンポーネントが同一規則で生成・破棄できる。 |
| FR-003 | `anchor` を廃止し、型付き `ref/handle` モデルへ置換する。 | Must | 参照先の型が静的に推論され、`any` 不要でアクセス可能。 |
| FR-004 | 宣言的UI記述をTSX記法へ統一する（トランスパイル前提）。 | Must | 公式サンプルがTSXのみで記述され、同一記法で主要機能を表現できる。 |
| FR-005 | JSXランタイムは提供せず、ビルド時トランスパイル済み成果物を実行対象とする。 | Must | 実行時に独自JSXランタイム依存が存在しない。 |
| FR-006 | 公開APIをTypeScriptネイティブ実装とし、`d.ts` 自動生成に統一する。 | Must | 手書き `d.ts` を廃止し、ビルド成果物から一貫生成される。 |
| FR-007 | 公開API上の `any` を禁止する。 | Must | `noImplicitAny` とAPI型検査で違反0件。 |
| FR-008 | レイアウト・描画・無効領域計算はC側主導で実行する。 | Must | フレームごとの通常描画でJSコールバック不要。 |
| FR-009 | JS実行点を初期化・イベント処理・状態遷移に限定する。 | Must | プロファイル上、アイドル描画中のJS実行が発生しない。 |
| FR-010 | 組み込み向けアニメーション機構を標準提供する。 | Must | 時間指定・イージング・開始/停止/完了通知が可能。 |
| FR-011 | アニメーションはCタイムライン駆動を基本とし、毎フレームJS実行を不要化する。 | Must | 標準アニメーションでフレームごとのJSハンドラ不要。 |
| FR-012 | イベント伝播モデルを明確化し、型付きイベントAPIを提供する。 | Must | 主要入力イベントが型安全に購読可能。 |
| FR-013 | リソース管理（画像/フォント/スタイル）を明示API化する。 | Should | 読み込み時期・解放時期が制御可能。 |
| FR-014 | 既存C資産をラップして利用可能にする拡張ポイントを提供する。 | Must | 既存ネイティブウィジェットを追加実装なしで接続可能。 |
| FR-015 | エラー診断を強化し、開発時に原因を特定しやすくする。 | Should | 代表的設定ミスで診断メッセージが出力される。 |

## 6. 非機能要件（NFR）

| ID | 要件 | 受け入れ基準 |
|---|---|---|
| NFR-001 | CPU効率は現行 `piu` 同等以上。 | 同一ベンチで平均CPU使用率が現行比 +5%以内。 |
| NFR-002 | メモリ効率は現行 `piu` 同等以上。 | 同一ベンチでピークRAMが現行比 +5%以内。 |
| NFR-003 | フレーム安定性を担保。 | 対象デバイスで60fps相当構成時に長時間ドロップフレーム率が定義閾値以下。 |
| NFR-004 | 起動性能を悪化させない。 | 初期表示時間が現行比 +10%以内。 |
| NFR-005 | バイナリサイズ増加を抑制。 | 主要構成で現行比 +15%以内。 |
| NFR-006 | 型安全性を保証。 | `strict: true`、`noImplicitAny: true`、公開API `any` 0件。 |
| NFR-007 | 再現性ある描画結果。 | 同入力で描画結果が安定し、ゴールデン差分が許容閾値内。 |
| NFR-008 | テスト自動化を必須化。 | CIで型検査・単体・描画回帰・性能計測が実行される。 |
| NFR-009 | 開発プロセスはテスト駆動開発（TDD）を採用する。 | 新規機能は失敗テスト追加→実装→リファクタの順でPR上に追跡可能。 |

## 7. アーキテクチャ要求
- 3層構成を採用する。
- UI宣言層はTypeScript/TSXで記述する（トランスパイル前提）。
- 状態管理は `signal` 中心モデルを採用し、更新伝播を最小単位で実行する。
- 実行計画層は軽量IRまたは等価構造へ変換する。
- 描画実行層はCランタイムでレイアウト・描画・アニメーションを処理する。
- JSランタイム依存はイベント処理と状態更新に限定する。
- ホットパス（毎フレーム処理）で追加割り当てを発生させない設計を原則とする。

## 8. API設計原則
- 命名は一貫性を最優先し、同種操作は同じ語彙と順序を使う。
- 暗黙状態を減らし、明示的な引数・戻り値で表現する。
- 参照取得は型付きハンドルへ統一する。
- ライフサイクルは作成・マウント・更新・破棄を明文化する。
- 破壊的変更はリリースノートと移行ガイドを必須化する。

## 9. 移行要件
- 既存 `piu` からの移行ガイドを提供する。
- `template`/`anchor` から新APIへの対応表を提供する。
- 機械変換可能な範囲はcodemodを提供する。
- 実験版のため後方互換性は保証しない。互換レイヤーは提供しない。

## 10. 品質保証要件
- TDDを標準プロセスとし、テストを先に作成してから実装する。
- 型テストで公開APIの破壊的変更を検知する。
- 描画ゴールデンテストで回帰を検知する。
- 実機ベンチマークでCPU/RAM/FPSを継続監視する。
- アニメーション回帰テストで時間精度と完了イベント順序を検証する。

## 11. リリース判定（MVP）
以下を全て満たした時点をMVPリリース条件とする。

- FR-001〜FR-004、FR-006〜FR-012を達成。
- NFR-001、NFR-002、NFR-006、NFR-008を達成。
- 代表画面3種以上で現行同等以上の性能を確認。
- 移行ガイド初版を公開。

## 12. 未決事項
- なし。
